spring:
  datasource:
    url: jdbc:h2:mem:db
    driver-class-name: org.h2.Driver
    username: sa
    initialization-mode: always
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true
  cloud:
    gcp:
      project-id: voltaire-290318

server:
  error:
    include-message: always

voltaire:
  http:
    user-info-header-name: X-Apigateway-Api-Userinfo
    api-key-header-name: API_KEY
  orders:
    delivery-timeout: 10

google:
  maps:
    geocode:
      url: https://maps.googleapis.com/maps/api/geocode/json?address=
    api-key: AIzaSyCfW9cgu7m6gVsvataXuYCsGnpIFCLVTMQ

